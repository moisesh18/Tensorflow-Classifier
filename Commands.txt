#COMANDO PARA HACER EL ENTRENAMIENTO

python retrain.py --image_dir imagenes

#Comando para hacer la clasificación de una imágen con la red neuronal:

python label_image.py --graph=/tmp/output_graph.pb --labels=/tmp
/output_labels.txt --input_layer=Placeholder --output_layer=final_result --image=[Aquí ponen la dirección de la imágen y quitan los corchetes]